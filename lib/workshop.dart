import 'package:flutter/material.dart';

class WorkShopPage extends StatelessWidget {
  const WorkShopPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("workshop page")));
  }
}
